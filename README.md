# 曹淋涵的个人主页

一个无需构建工具的静态学术主页，可直接部署到 GitHub Pages。

## 本地预览

在此目录执行：

```bash
python3 -m http.server 8000
```

然后访问 `http://localhost:8000`。

## 发布

将仓库命名为 `clh124.github.io`，推送到 GitHub 后，在仓库 Settings → Pages 中选择从默认分支根目录发布。
